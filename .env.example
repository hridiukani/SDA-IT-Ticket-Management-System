# ============================================
# IT Ticket Management System
# Environment Configuration Template
# ============================================

# PostgreSQL Database Configuration
POSTGRES_DB=ticketdb
POSTGRES_USER=ticketuser
POSTGRES_PASSWORD=ticketpass
POSTGRES_PORT=5433

# Database Connection URL (for Spring Boot)
DATABASE_URL=jdbc:postgresql://localhost:5433/ticketdb
DB_USERNAME=ticketuser
DB_PASSWORD=ticketpass

# pgAdmin Configuration
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Spring Boot Configuration
SERVER_PORT=8080
SPRING_PROFILE=dev

# JWT Configuration
# IMPORTANT: Generate a strong random secret for production
# You can generate one using: openssl rand -base64 64
JWT_SECRET=your-256-bit-secret-key-change-this-in-production-use-strong-random-key
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200

# Mail Configuration (Gmail example)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# File Upload Configuration
FILE_UPLOAD_DIR=./uploads

# ============================================
# Instructions:
# 1. Copy this file to .env
# 2. Update the values with your actual configuration
# 3. Never commit .env to version control
# ============================================
